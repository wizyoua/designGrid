$(window).load(function() {

    setTimeout(function(){

        $(".se-pre-con").fadeOut("slow");;
        
    },4000);  // 2000 is the delay in milliseconds
    // Animate loader off screen
  });

$(document).ready(function() {
setTimeout(function(){
$('.js-dropdown-item').dropdownLayer();
},3000);  // 2000 is the delay in milliseconds
});
